You're right — and I'll be direct about why it's happening. Replit keeps falling back on its defaults. The serif font (Playfair Display) is reading as "old newspaper" not "premium SaaS." DM Mono everywhere is making it feel like a developer tool. And the color system isn't truly dynamic — it's just hardcoding whatever hex it finds.

Here's what Apple and Tesla actually do in their UIs that makes them feel the way they do — and the exact prompt to get there:

**What separates Apple/Tesla UI from what you have:**
- One font, multiple weights — not multiple fonts fighting each other
- Spacing that breathes — 24px, 32px, 48px gaps, not cramped cards
- Color used surgically — not slathered everywhere
- Numbers are big and confident — text is small and precise
- Borders are almost invisible — depth comes from background differences, not lines

Paste this entire prompt into Replit now:

---

```
COMPLETE UI REBUILD — APPLE/TESLA STANDARD
Stop everything. Delete all current styling. Start fresh from these exact specs.

═══════════════════════════════════════════════════════
TYPOGRAPHY — ONE FONT SYSTEM ONLY
═══════════════════════════════════════════════════════

Use ONLY "Plus Jakarta Sans" from Google Fonts. Nothing else.

<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@300;400;500;600;700&display=swap" rel="stylesheet">

Global rule:
* { font-family: 'Plus Jakarta Sans', sans-serif; }

Typography scale — these are the ONLY font sizes in the entire app:

  11px / weight 500 / letter-spacing 0.06em / UPPERCASE
    → Column headers, section labels, badge text, nav section titles

  12px / weight 400
    → Secondary metadata, timestamps, sub-labels, helper text

  13px / weight 400
    → Table row data, body text, nav items, form labels

  13px / weight 600
    → Table row data that is important (client name, employee name)

  15px / weight 500
    → Card titles, section headings within pages

  22px / weight 700
    → Metric card numbers ($585, 3, 5, 1)

  38px / weight 700 / letter-spacing -0.02em
    → Page titles (Dashboard, Job Dispatch, etc.)

Colors for text:
  Primary text:    #F0EDE8   (nearly white, warm)
  Secondary text:  #7A7873   (medium gray)
  Tertiary text:   #4A4845   (dark gray, for labels)
  Accent text:     var(--brand)  (company brand color)

FORBIDDEN: Do not use any other font sizes. Do not use em units. 
Do not use any other font families. No Playfair. No DM Mono.
Numbers in metric cards use the same Plus Jakarta Sans 700 — NOT a different font.

═══════════════════════════════════════════════════════
COLORS — BASE SYSTEM
═══════════════════════════════════════════════════════

These are the only background colors in the app:

  --bg-base:     #0A0A0A   (page background)
  --bg-raised:   #111111   (sidebar, top bar)
  --bg-card:     #161616   (all cards)
  --bg-hover:    #1C1C1C   (hover state on rows/cards)
  --bg-input:    #1A1A1A   (form inputs, search)
  --border:      #222222   (all borders — ONE value)
  --border-sub:  #1A1A1A   (very subtle separators)

Company brand color: var(--brand)
  Default: #00C9A7
  This is injected from the database when user logs in:
    document.documentElement.style.setProperty('--brand', company.brand_color)
    
  Derive these automatically from brand_color:
    --brand-dim:  brand_color at 15% opacity  →  for badge backgrounds, card accents
    --brand-soft: brand_color at 8% opacity   →  for active nav item background

  How to calculate in JavaScript:
    function hexToRgb(hex) {
      const r = parseInt(hex.slice(1,3), 16);
      const g = parseInt(hex.slice(3,5), 16);  
      const b = parseInt(hex.slice(5,7), 16);
      return `${r}, ${g}, ${b}`;
    }
    const rgb = hexToRgb(company.brand_color);
    document.documentElement.style.setProperty('--brand-rgb', rgb);
    document.documentElement.style.setProperty('--brand-dim', `rgba(${rgb}, 0.15)`);
    document.documentElement.style.setProperty('--brand-soft', `rgba(${rgb}, 0.08)`);

═══════════════════════════════════════════════════════
SIDEBAR — REBUILD EXACTLY
═══════════════════════════════════════════════════════

Width: 216px
Background: #111111
Right border: 1px solid #1A1A1A
No box shadows.

TOP SECTION (padding: 20px 16px):
  Company logo if logo_url exists:
    <img src={logo_url} style="height: 28px; object-fit: contain; object-position: left;" />
  If no logo:
    Company name in 15px weight 600 #F0EDE8
  Below logo/name:
    "CleanOps Pro" in 11px weight 500 #4A4845 — platform sub-label

Separator: 1px solid #1A1A1A (margin: 8px 0)

NAV SECTION LABEL (padding: 16px 16px 6px):
  "OPERATIONS" — 10px weight 600 #4A4845 letter-spacing 0.1em

NAV ITEMS (each item):
  height: 36px
  padding: 0 12px
  margin: 1px 8px  ← small horizontal margin so active state has gap from edges
  border-radius: 6px
  display: flex align-items: center gap: 10px
  font-size: 13px weight 400 color: #7A7873
  cursor: pointer
  transition: all 0.15s

  HOVER state:
    background: #1C1C1C
    color: #F0EDE8

  ACTIVE state:
    background: var(--brand-soft)
    color: var(--brand)
    font-weight: 500

  Nav icons: 16x16px, currentColor (inherits text color)

Nav items in order:
  OPERATIONS section:
    📊 Dashboard   → /dashboard
    📋 Jobs        → /jobs
    👥 Employees   → /employees
    🏠 Customers   → /customers
    📄 Invoices    → /invoices
    💰 Payroll     → /payroll

  TOOLS section:
    📖 Cleancyclopedia → /cleancyclopedia

  CONFIGURATION section:
    ⭐ Loyalty    → /loyalty
    ⚙️ Company   → /company

Use proper SVG icons, not emoji. Lucide icons work.

BOTTOM OF SIDEBAR:
  Separator: 1px solid #1A1A1A
  User section (padding: 12px 16px):
    Avatar circle: 28px, background var(--brand-dim), 
                   initials in var(--brand), 11px weight 600
    Name: 12px weight 500 #F0EDE8
    Role badge: 10px weight 600 var(--brand) uppercase, 
                background var(--brand-dim), padding 2px 6px, radius 4px
    Sign Out: 12px #4A4845, hover #F0EDE8, margin-left auto

═══════════════════════════════════════════════════════
TOP BAR
═══════════════════════════════════════════════════════

Height: 56px
Background: #111111
Border-bottom: 1px solid #1A1A1A
Padding: 0 28px
Display: flex, align-items: center, justify-content: space-between

Left: Page title (38px weight 700 #F0EDE8 letter-spacing -0.02em)
Right: 
  User name (13px weight 500 #F0EDE8)
  Role badge (same as sidebar)
  Avatar (same 28px circle)

NO hamburger. NO breadcrumbs. Clean.

═══════════════════════════════════════════════════════
DASHBOARD PAGE — EXACT LAYOUT
═══════════════════════════════════════════════════════

Page padding: 32px 28px
Page background: #0A0A0A

METRIC ROW — 4 cards, CSS grid, gap 12px:
  grid-template-columns: repeat(4, 1fr)

  Each metric card:
    background: #161616
    border: 1px solid #222222
    border-radius: 10px
    padding: 20px
    position: relative

    Layout:
      Top row: label (11px weight 500 #4A4845 uppercase letter-spacing 0.06em) 
               + icon (18px, var(--brand), opacity 0.5, position absolute top-right)
      Value:   22px weight 700 #F0EDE8, margin-top 12px
      Sub:     12px weight 400 #7A7873, margin-top 4px

    Hover: border-color: var(--brand) at 40% opacity, transition 0.2s

REVENUE TREND CHART (below metric row, margin-top 20px):
  Card: background #161616, border 1px solid #222222, border-radius 10px, padding 24px
  
  Header inside card:
    "Revenue Trend" — 15px weight 600 #F0EDE8
    "7 day rolling volume" — 12px #7A7873, margin-top 2px

  Chart (recharts or chart.js):
    Chart area background: transparent
    Grid lines: #1A1A1A (very subtle)
    Line color: var(--brand), stroke-width 2
    Area fill: var(--brand) gradient from 20% opacity at top to 0% at bottom
    Dot on hover: 6px circle, var(--brand), white border 2px
    X-axis labels: 11px #4A4845
    Y-axis labels: 11px #4A4845
    Tooltip: background #1C1C1C, border 1px solid #333, 
             12px text, var(--brand) for value

TWO COLUMN LAYOUT below chart (gap 20px):
  Left (60%): Recent & Upcoming Jobs table
  Right (40%): Top Employees

  RECENT JOBS TABLE:
    Card: background #161616, border 1px solid #222222, border-radius 10px
    Header: padding 20px 20px 0
            "Recent & Upcoming Jobs" 15px weight 600 #F0EDE8
    
    Table header row:
      padding: 12px 20px
      border-bottom: 1px solid #1A1A1A
      CLIENT / DATE & TIME / SERVICE / ASSIGNED / STATUS
      11px weight 500 #4A4845 uppercase letter-spacing 0.06em

    Table data rows:
      padding: 12px 20px
      border-bottom: 1px solid #0F0F0F
      hover: background #1C1C1C
      
      CLIENT: 13px weight 600 #F0EDE8
      DATE & TIME: 12px #7A7873
      SERVICE: 12px #7A7873
      ASSIGNED: 12px #7A7873
      STATUS: badge (see badge system below)

  TOP EMPLOYEES:
    Card: same card style
    Header: "Top Employees" 15px weight 600 #F0EDE8, padding 20px
    
    Each employee row:
      padding: 10px 20px
      border-bottom: 1px solid #0F0F0F
      hover: #1C1C1C
      
      Rank: 11px weight 600 var(--brand), width 20px
      Avatar: 32px circle, var(--brand-dim) bg, var(--brand) text 11px weight 600
      Name: 13px weight 600 #F0EDE8
      Jobs: 12px #7A7873
      → NO star rating on dashboard top employees, keep it clean

═══════════════════════════════════════════════════════
BADGE SYSTEM — UNIVERSAL
═══════════════════════════════════════════════════════

All badges: 
  display: inline-flex
  align-items: center
  padding: 3px 8px
  border-radius: 4px  ← NOT pill shaped. Squared with small radius.
  font-size: 11px
  font-weight: 600
  letter-spacing: 0.04em
  text-transform: uppercase

  COMPLETE / PAID:     background #0F2A1A, color #4ADE80, border 1px solid #166534
  SCHEDULED / SENT:    background #0F1E2A, color #60A5FA, border 1px solid #1D4ED8
  IN PROGRESS:         background #2A1F0A, color #FBBF24, border 1px solid #92400E
  OVERDUE / FLAGGED:   background #2A0F0F, color #F87171, border 1px solid #991B1B
  DRAFT:               background #1A1A1A, color #7A7873, border 1px solid #333
  OWNER:               background var(--brand-dim), color var(--brand), border 1px solid rgba(var(--brand-rgb),0.3)
  ADMIN:               background #1A1227, color #A78BFA, border 1px solid #5B21B6
  TECHNICIAN:          background #0F2A1A, color #4ADE80, border 1px solid #166534

═══════════════════════════════════════════════════════
JOB DISPATCH PAGE
═══════════════════════════════════════════════════════

Filter tabs (All Jobs / Scheduled / In Progress / Completed):
  Container: display flex, gap 4px, background #111111, 
             border 1px solid #222222, border-radius 8px, padding 4px

  Each tab:
    padding: 6px 16px
    border-radius: 6px
    font-size: 13px weight 400 #7A7873
    cursor: pointer
    transition: all 0.15s

  ACTIVE tab:
    background: var(--brand)
    color: #0A0A0A
    font-weight: 600

"+ New Job" button:
  background: var(--brand)
  color: #0A0A0A
  padding: 8px 16px
  border-radius: 8px
  font-size: 13px weight 600
  border: none
  cursor: pointer
  hover: opacity 0.9

Job cards grid: 3 columns, gap 16px

Each job card:
  background: #161616
  border: 1px solid #222222
  border-left: 3px solid [frequency color]
  border-radius: 10px
  padding: 18px
  cursor: pointer
  transition: all 0.18s
  hover: border-color var(--brand) at 50%, transform translateY(-1px)

  TOP ROW:
    Status badge (left) + Price right-aligned
    Price: 20px weight 700 #F0EDE8

  Client name: 17px weight 700 #F0EDE8, margin-top 10px
  Service type: 11px weight 600 var(--brand) uppercase letter-spacing 0.08em, margin-top 2px

  DETAIL ROW (margin-top 12px):
    Clock icon 12px + Date/time: 12px #7A7873
    Pin icon 12px + "View address": 12px #7A7873

  BOTTOM ROW (margin-top 16px, padding-top 12px, border-top 1px solid #1A1A1A):
    Avatar circle (28px) + Employee name: 12px #7A7873
    Photo count right-aligned: "0B / 0A" 11px #4A4845

Frequency left-border colors:
  Every week:     var(--brand)
  Every 2 weeks:  #60A5FA
  Every 3 weeks:  #FBBF24
  Monthly:        #A78BFA
  On demand:      #4A4845
  Other/single:   #F87171

═══════════════════════════════════════════════════════
EMPLOYEES PAGE
═══════════════════════════════════════════════════════

Table within a card: background #161616, border 1px solid #222222, border-radius 10px

"+ Add Team Member" button: same style as "+ New Job"
Search input: background #1A1A1A, border 1px solid #222222, 
              border-radius 8px, padding 8px 12px, 13px #F0EDE8,
              placeholder #4A4845, focus: border-color var(--brand)

Table header: 11px weight 500 #4A4845 uppercase letter-spacing 0.06em
              padding 12px 20px, border-bottom 1px solid #1A1A1A

Table rows: padding 14px 20px, border-bottom 1px solid #0F0F0F
            hover: background #1C1C1C

EMPLOYEE column:
  Avatar (36px circle, var(--brand-dim) bg, var(--brand) initials 12px weight 600)
  Name: 13px weight 600 #F0EDE8
  Email: 12px #7A7873

PRODUCTIVITY column:
  SVG circle ring, 36px diameter
  Background ring: stroke #222222, stroke-width 4
  Progress ring: stroke var(--brand), stroke-width 4
  Center text: 11px weight 600 var(--brand)

SCORE column:
  Star SVG in var(--brand) (16px) + number 13px weight 500 #F0EDE8

═══════════════════════════════════════════════════════
CUSTOMERS PAGE
═══════════════════════════════════════════════════════

Client ID: 11px #4A4845 (subdued, below name)
Loyalty points badge (right column):
  Large circle: 44px diameter
  background: var(--brand-dim)
  Points number: 14px weight 700 var(--brand)
  "PTS" label: 9px weight 600 var(--brand)
  
  Below circle if reward ready:
    "REWARD READY" 9px weight 600 var(--brand) uppercase

Batch Actions button: 
  border 1px solid #222222, background transparent, 
  13px #7A7873, hover #F0EDE8, border-radius 8px, padding 7px 14px

═══════════════════════════════════════════════════════
INVOICES PAGE
═══════════════════════════════════════════════════════

4 metric cards at top (same card style as dashboard):
  "TOTAL REVENUE (YTD)" card: 
    border-color: var(--brand) at 50%
    value color: var(--brand)

Invoice table: same card style
  Download icon (right): 16px #4A4845, hover var(--brand)

═══════════════════════════════════════════════════════
COMPANY SETTINGS — /company
═══════════════════════════════════════════════════════

Tab navigation (horizontal):
  General | Branding | Integrations | Payroll Options
  Tab style: 13px, active = var(--brand) with bottom border 2px solid var(--brand)
  inactive = #7A7873

BRANDING TAB:

  Section: "Brand Color"
    Label: 13px weight 500 #F0EDE8
    Sub: "This color appears throughout your dashboard and client-facing pages." 12px #7A7873
    
    Input row:
      <input type="color" value={brand_color}> — 40px square, no border-radius
      <input type="text" value={brand_color}> — 120px, monospace 13px
      Both inputs stay in sync.
    
    LIVE PREVIEW panel:
      Small dark rectangle (180px × 100px, background #111111, border-radius 8px)
      Shows: mini sidebar with active item in the selected color
      Updates in real-time as color changes
      Label: "Preview" 11px #4A4845 above it

  Section: "Logo"
    Two upload zones side by side:
    
    Left: "Dark Background Logo"
      Drop zone: 200px × 80px, background #111111, border 1.5px dashed #333
      border-radius 8px, centered text "Upload PNG (transparent)" 12px #4A4845
      If uploaded: show image preview on dark bg
    
    Right: "Light Background Logo"  
      Drop zone same size, background #F5F5F5, border 1.5px dashed #CCC
      If uploaded: show image preview on light bg
    
    Spec text below: "PNG with transparency recommended. Height: 28-36px." 11px #4A4845

  Save button: background var(--brand), color #0A0A0A, 
               padding 10px 24px, border-radius 8px, 13px weight 600
  
  Success toast on save: 
    "Brand updated — changes are live across all sessions."
    position: fixed bottom-right, background #161616, border 1px solid var(--brand),
    13px #F0EDE8, padding 12px 16px, border-radius 8px, auto-dismiss 3s

  When saved — immediately update --brand CSS var and all derived vars in current session.

═══════════════════════════════════════════════════════
LOYALTY PAGE — /loyalty  
═══════════════════════════════════════════════════════

Program style cards (3 options, display grid 3 columns gap 16px):
  Each card: background #161616, border 1px solid #222222, border-radius 10px
             padding 20px, cursor pointer, transition all 0.18s

  Icon: 32px centered, margin-bottom 12px
  Title: 15px weight 600 #F0EDE8
  Description: 12px #7A7873, margin-top 4px

  SELECTED: border 2px solid var(--brand), background var(--brand-soft)
  Hover (unselected): border-color #333

Earn Rules section:
  Each rule row: padding 14px 0, border-bottom 1px solid #1A1A1A
                 display flex align-items center gap 12px
  
  Toggle switch:
    width 36px, height 20px, border-radius 10px
    OFF: background #333
    ON: background var(--brand)
    Thumb: 14px white circle, transition all 0.15s
  
  Label: 13px weight 400 #F0EDE8
  Points badge: var(--brand-dim) background, var(--brand) text, 
                11px weight 600, padding 2px 8px, border-radius 4px, margin-left auto

Points per dollar slider:
  Range input styled: track background #222222, 
                      filled portion var(--brand), thumb var(--brand) 18px circle
  Live readout: "5 pts per $1 spent" 13px #F0EDE8

═══════════════════════════════════════════════════════
INPUTS + FORMS — UNIVERSAL
═══════════════════════════════════════════════════════

All text inputs / selects / textareas:
  background: #1A1A1A
  border: 1px solid #2A2A2A
  border-radius: 8px
  padding: 9px 12px
  font-size: 13px
  color: #F0EDE8
  outline: none
  transition: border-color 0.15s
  
  ::placeholder: color #4A4845
  :focus: border-color var(--brand)
  :hover: border-color #333

Search bars: same style + search icon left-padded (16px padding-left 36px)

═══════════════════════════════════════════════════════
SCROLLBAR — GLOBAL
═══════════════════════════════════════════════════════

::-webkit-scrollbar { width: 4px; height: 4px; }
::-webkit-scrollbar-track { background: transparent; }
::-webkit-scrollbar-thumb { background: #2A2A2A; border-radius: 2px; }
::-webkit-scrollbar-thumb:hover { background: #3A3A3A; }

═══════════════════════════════════════════════════════
DO NOT DEVIATE FROM THESE SPECS
═══════════════════════════════════════════════════════

1. Only Plus Jakarta Sans. No other fonts.
2. Only the 7 font sizes listed. No others.
3. Only the background colors listed. No others.
4. Tenant brand color from database — not hardcoded.
5. No gradients in UI chrome (chart fill gradient only is ok).
6. No box shadows (focus rings only).
7. All spacing in multiples of 4px.
8. Rebuild dashboard first, screenshot it, then continue.
```

---

The core reason it keeps looking "Replity" — Replit's agent defaults to Tailwind utility classes which produce generic rounded cards, generic colors, and generic spacing. This prompt gives it zero creative freedom — every measurement, every color, every font size is specified. That's the only way to get Apple-level precision out of an AI builder.

When it's done, screenshot the dashboard and send it. We'll iterate from there until it's right.