kind = "web"
previewPath = "/"
title = "Qleno"
version = "1.0.0"
id = "artifacts/cleanops-pro"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
localPort = 19325
name = "web"
paths = ["/"]

[services.development]
run = "pnpm --filter @workspace/cleanops-pro run dev"

[services.env]
PORT = "19325"
BASE_PATH = "/"

[services.production]
build = ["pnpm", "--filter", "@workspace/api-server", "run", "build"]
run = ["pnpm", "--filter", "@workspace/api-server", "run", "start"]

[services.production.health.startup]
path = "/api/healthz"
